public class TheRadioButton {

    // JRadioButton = One or more buttons in a grouping in which only one may be
    // selected per group.
    public static void main(String[] args) {
        new RadioButtonFrame();
    }
}